logging:
  level:
    org:
      #flywaydb: debug

spring:
  debug: true
  flyway:
    cleanDisabled: false
    locations: filesystem:./src/test/resources/db/migration,filesystem:./src/main/resources/db/migration